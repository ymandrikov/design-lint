
No color or backgroundColor in style= props (1)
  src/app.tsx:9  color in style= — move to a CSS module

No raw color values in component CSS. Use semantic tokens instead (3)
  src/components/card.css:2  Raw color value #ff0000 — use a var(--color-*) token
  src/app.tsx:9  #ff0000 — raw color value; use a var(--color-*) token
  src/app.tsx:12  bg-[#123456] — raw color in arbitrary value; use a var(--color-*) token

No opacity modifiers on color classes. Add a semantic token instead (1)
  src/app.tsx:15  bg-primary/50 — opacity modifier on color class; use a dedicated token

No spectral (palette) Tailwind color classes. Use semantic tokens instead (2)
  src/components/card.css:3  bg-red-500 — spectral color class; use a design token instead — try bg-danger
  src/app.tsx:18  text-green-500 — spectral color class; use a design token instead — try text-success-content

Color class violates token constraints (2)
  src/app.tsx:21  text-muted — muted not allowed for text- (allowed: *foreground*, *content*, primary, link*)
  src/app.tsx:24  hover:bg-primary — hover: color classes must use a -hover suffixed token (use bg-primary-hover)

Don't use dark: variant. Use semantic tokens instead (1)
  src/app.tsx:27  dark:bg-primary — dark: variant not allowed; use a semantic token (dark theme is handled via CSS custom properties)

Don't use hover: on non-interactive elements (1)
  src/app.tsx:30  hover: on non-interactive <div> — remove hover: or use an interactive element

Don't override color className on component. Choose from the variants, or create a new one (1)
  src/app.tsx:33  bg-success overrides color on <Button> — add a variant instead

Color class references a token not defined in the color token files (1)
  src/app.tsx:36  bg-nonexistent — nonexistent is not defined; check spelling or add --color-nonexistent

13 violations found.
  (1 line suppressed with color-lint-ignore)
